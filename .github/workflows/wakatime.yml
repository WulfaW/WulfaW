name: Update WakaTime Stats SVG
on:
  schedule:
    - cron: "0 */6 * * *" # Her 6 saatte bir uyanır ve günceller
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-stats:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Generate SVG from Vercel API
        env:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p wakatime
          # Linkin sonuna &theme=cyber_aqua eklendi!
          curl -fsSL "https://wakatime-readme-stats.vercel.app/api/wakatimeStats?username=Wulfa&api_key=$WAKATIME_API_KEY&github_token=$GITHUB_TOKEN&theme=cyber_aqua" \
          -o wakatime/stats.svg

      - name: Bump README cache-buster for wakatime/stats.svg (daily)
        run: |
          set -euo pipefail
          V="$(date -u +%Y%m%d)"
          sed -i -E "s#(wakatime/stats\.svg)\?v=[0-9]+#\1?v=$V#g" README.md
          sed -i -E "s#(wakatime/stats\.svg)(\"|')#\1?v=$V\2#g" README.md

      - name: Commit changes to repo
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add wakatime/stats.svg README.md
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "Update WakaTime stats SVG"
          git push
